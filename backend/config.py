# ── 점수 계산 가중치 (합계 = 1.0) ─────────────────────────────────────────────
W_USAGE_RATE      = 0.20   # 사용률
W_SURVIVAL_RATE   = 0.30   # 생존율
W_COMBAT_SURVIVAL = 0.20   # 교전 생존율
W_WIN_RATE        = 0.20   # 우승 기여율
W_MOVE_SUCCESS    = 0.10   # 이동 성공률

# ── 격자 설정 ────────────────────────────────────────────────────────────────
GAME_MAP_SIZE  = 816000    # cm (에란겔 전체 크기)
GRID_CELL_SIZE = 5000      # cm (50m × 50m 격자)
GRID_CELLS     = 163       # ceil(816000 / 5000) ≈ 163

# ── 자기장 유사도 허용 범위 ───────────────────────────────────────────────────
POS_TOLERANCE    = 50000   # cm (중심 거리 최대 500m)
RADIUS_TOLERANCE = 0.20    # ±20%

# ── 신뢰도 기준 ──────────────────────────────────────────────────────────────
MIN_SAMPLES_CONFIDENCE = 30   # 이 수치 미만이면 low_confidence = True
TOP_N_CELLS            = 10   # 반환할 상위 격자 수
