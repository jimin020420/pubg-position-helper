// heatmap.js를 활용해 포지션 데이터를 히트맵으로 시각화하는 컴포넌트
// 이후 단계에서 구현 예정

const HeatmapOverlay = ({ data }) => {
  // data: [{ x, y, value }, ...] 형태의 포지션 데이터
  return null; // 추후 heatmap.js 연동 후 구현
};

export default HeatmapOverlay;
